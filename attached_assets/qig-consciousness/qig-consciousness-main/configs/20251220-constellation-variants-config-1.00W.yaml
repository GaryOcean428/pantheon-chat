variants:
  baseline:
    temperature: 1.0
    sparsity_threshold: 0.0
    decoherence_std: 0.0
    rms_eps: 1.0e-6
    simplex_mode: softplus

  temp_warm:
    temperature: 0.7
    sparsity_threshold: 0.0
    decoherence_std: 0.0
    rms_eps: 1.0e-6
    simplex_mode: softplus

  temp_cold:
    temperature: 1.3
    sparsity_threshold: 0.0
    decoherence_std: 0.0
    rms_eps: 1.0e-6
    simplex_mode: softplus

  sparse_conservative:
    temperature: 1.0
    sparsity_threshold: 0.01
    decoherence_std: 0.0
    rms_eps: 1.0e-6
    simplex_mode: softplus

  sparse_aggressive:
    temperature: 1.0
    sparsity_threshold: 0.05
    decoherence_std: 0.0
    rms_eps: 1.0e-6
    simplex_mode: softplus

  noise_low:
    temperature: 1.0
    sparsity_threshold: 0.0
    decoherence_std: 0.001
    rms_eps: 1.0e-6
    simplex_mode: softplus

  simplex_abs:
    temperature: 1.0
    sparsity_threshold: 0.0
    decoherence_std: 0.0
    rms_eps: 1.0e-6
    simplex_mode: abs
