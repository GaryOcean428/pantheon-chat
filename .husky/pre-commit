#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run type check
npm run check

# Run linting (with auto-fix disabled to prevent modifying staged files)
npm run lint

# Run tests (fast subset)
npm test -- --run --reporter=dot
