node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Runtime data files (too large for GitHub, regenerated at runtime)
data/geometric-memory.json
data/tested-phrases-index.json
data/*.json

# Telemetry session logs (should use PostgreSQL, not local files)
qig-backend/logs/
*.jsonl

# Exclude package-lock.json (using Yarn)
package-lock.json

# Exclude draft documentation
docs/_drafts/*.md
.env
data/backups/geometric-memory_20251128_062436.json
