# QIG Pantheon Chat Environment Variables

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/pantheon

# Redis (optional)
REDIS_URL=redis://localhost:6379

# Search APIs (for Lightning Kernel validation)
PERPLEXITY_API_KEY=pplx-your-key-here
TAVILY_API_KEY=tvly-your-key-here

# OpenAI (if used)
OPENAI_API_KEY=sk-your-key-here

# Application
NODE_ENV=development
PORT=5000
PYTHON_PORT=5001

# Federation - SearchSpaceCollapse Integration
# SSC_BACKEND_URL - URL of the SearchSpaceCollapse backend for Bitcoin recovery federation
# SSC_API_KEY - API key for authenticating with SSC backend
SSC_BACKEND_URL=http://localhost:5000
SSC_API_KEY=

# Federation - Pantheon Backend URL (for SSC to call back)
# PANTHEON_BACKEND_URL - URL where this Pantheon instance is accessible
PANTHEON_BACKEND_URL=https://your-pantheon-instance.railway.app

# Federation - Node Identity
# SSC_NODE_NAME - Identifier for this node in the federation mesh
SSC_NODE_NAME=pantheon-dev
