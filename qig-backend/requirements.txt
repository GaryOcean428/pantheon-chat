flask>=3.0.0
flask-cors>=4.0.0
numpy>=1.24.0
scipy>=1.11.0
psycopg2-binary>=2.9.9
torch>=2.0.0
pypdf>=4.0.0
openai>=1.0.0
anthropic>=0.18.0

# Celery for async training tasks
celery[redis]>=5.3.0
redis>=5.0.0

# QIG Tokenizer (local editable install)
# Install with: pip install -e ../../qig-tokenizer
# Provides: qiggraph, qig_tokenizer, coordizers
