/**
 * @unused This component is currently not used in the application.
 * It is kept as part of the shadcn/ui component library for future use.
 */
import * as AspectRatioPrimitive from "@radix-ui/react-aspect-ratio"

const AspectRatio = AspectRatioPrimitive.Root

export { AspectRatio }
